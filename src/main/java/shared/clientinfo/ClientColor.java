package shared.clientinfo;

import java.io.Serializable;

/**
 * It's a copy of the color enum in the server model, it doesn't contains any logic
 */
public enum ClientColor implements Serializable {
    VIOLET, BLUE, RED, YELLOW, GREEN
}
