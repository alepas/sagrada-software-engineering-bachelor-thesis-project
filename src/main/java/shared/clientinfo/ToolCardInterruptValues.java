package shared.clientinfo;

import java.io.Serializable;

/**
 * contains all possible value that the player could receive when a toolcard is interrupt
 */
public enum ToolCardInterruptValues implements Serializable {
    YES,
    NO,
    OK
}
